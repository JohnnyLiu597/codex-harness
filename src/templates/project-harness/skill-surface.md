# Skill Surface

Project skill surface reviews are advisory and read-only by default.

```powershell
.\scripts\audit-skill-surface.ps1
```

Archive or restore skills only when the project actually needs the active
surface to change.
