{
  "description": "Privacy-safe lifecycle guardrails for the Codex-only harness.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "^(startup|resume|clear|compact)$",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NoProfile -ExecutionPolicy Bypass -Command '$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $HOME \".codex\" }; & (Join-Path $codexHome \"scripts/codex-hook.ps1\") -Event SessionStart'",
            "commandWindows": "powershell.exe -NoProfile -ExecutionPolicy Bypass -Command \"$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $env:USERPROFILE '.codex' }; & (Join-Path $codexHome 'scripts\\codex-hook.ps1') -Event SessionStart\"",
            "timeout": 8,
            "additionalContextLimit": 500
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "^.*$",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NoProfile -ExecutionPolicy Bypass -Command '$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $HOME \".codex\" }; & (Join-Path $codexHome \"scripts/codex-hook.ps1\") -Event PreToolUse'",
            "commandWindows": "powershell.exe -NoProfile -ExecutionPolicy Bypass -Command \"$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $env:USERPROFILE '.codex' }; & (Join-Path $codexHome 'scripts\\codex-hook.ps1') -Event PreToolUse\"",
            "timeout": 8
          }
        ]
      }
    ],
    "PermissionRequest": [
      {
        "matcher": "^.*$",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NoProfile -ExecutionPolicy Bypass -Command '$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $HOME \".codex\" }; & (Join-Path $codexHome \"scripts/codex-hook.ps1\") -Event PermissionRequest'",
            "commandWindows": "powershell.exe -NoProfile -ExecutionPolicy Bypass -Command \"$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $env:USERPROFILE '.codex' }; & (Join-Path $codexHome 'scripts\\codex-hook.ps1') -Event PermissionRequest\"",
            "timeout": 8
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "^(Bash|apply_patch|Edit|Write)$",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NoProfile -ExecutionPolicy Bypass -Command '$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $HOME \".codex\" }; & (Join-Path $codexHome \"scripts/codex-hook.ps1\") -Event PostToolUse'",
            "commandWindows": "powershell.exe -NoProfile -ExecutionPolicy Bypass -Command \"$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $env:USERPROFILE '.codex' }; & (Join-Path $codexHome 'scripts\\codex-hook.ps1') -Event PostToolUse\"",
            "timeout": 8
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NoProfile -ExecutionPolicy Bypass -Command '$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $HOME \".codex\" }; & (Join-Path $codexHome \"scripts/codex-hook.ps1\") -Event PreCompact'",
            "commandWindows": "powershell.exe -NoProfile -ExecutionPolicy Bypass -Command \"$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $env:USERPROFILE '.codex' }; & (Join-Path $codexHome 'scripts\\codex-hook.ps1') -Event PreCompact\"",
            "timeout": 8
          }
        ]
      }
    ],
    "PostCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NoProfile -ExecutionPolicy Bypass -Command '$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $HOME \".codex\" }; & (Join-Path $codexHome \"scripts/codex-hook.ps1\") -Event PostCompact'",
            "commandWindows": "powershell.exe -NoProfile -ExecutionPolicy Bypass -Command \"$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $env:USERPROFILE '.codex' }; & (Join-Path $codexHome 'scripts\\codex-hook.ps1') -Event PostCompact\"",
            "timeout": 8
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NoProfile -ExecutionPolicy Bypass -Command '$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $HOME \".codex\" }; & (Join-Path $codexHome \"scripts/codex-hook.ps1\") -Event UserPromptSubmit'",
            "commandWindows": "powershell.exe -NoProfile -ExecutionPolicy Bypass -Command \"$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $env:USERPROFILE '.codex' }; & (Join-Path $codexHome 'scripts\\codex-hook.ps1') -Event UserPromptSubmit\"",
            "timeout": 8
          }
        ]
      }
    ],
    "SubagentStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NoProfile -ExecutionPolicy Bypass -Command '$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $HOME \".codex\" }; & (Join-Path $codexHome \"scripts/codex-hook.ps1\") -Event SubagentStart'",
            "commandWindows": "powershell.exe -NoProfile -ExecutionPolicy Bypass -Command \"$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $env:USERPROFILE '.codex' }; & (Join-Path $codexHome 'scripts\\codex-hook.ps1') -Event SubagentStart\"",
            "timeout": 8,
            "additionalContextLimit": 500
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NoProfile -ExecutionPolicy Bypass -Command '$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $HOME \".codex\" }; & (Join-Path $codexHome \"scripts/codex-hook.ps1\") -Event SubagentStop'",
            "commandWindows": "powershell.exe -NoProfile -ExecutionPolicy Bypass -Command \"$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $env:USERPROFILE '.codex' }; & (Join-Path $codexHome 'scripts\\codex-hook.ps1') -Event SubagentStop\"",
            "timeout": 8
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NoProfile -ExecutionPolicy Bypass -Command '$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $HOME \".codex\" }; & (Join-Path $codexHome \"scripts/codex-hook.ps1\") -Event Stop'",
            "commandWindows": "powershell.exe -NoProfile -ExecutionPolicy Bypass -Command \"$codexHome = if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $env:USERPROFILE '.codex' }; & (Join-Path $codexHome 'scripts\\codex-hook.ps1') -Event Stop\"",
            "timeout": 8
          }
        ]
      }
    ]
  }
}
