version = 1
id = "harness"
kind = "cron"
name = "Weekly Codex Harness Review"
prompt = "Maintain the installed Codex-only harness. Read __CODEX_HOME_TOML_CONTENT__\\AGENTS.md, __CODEX_HOME_TOML_CONTENT__\\CODEX.md, and the project-harness-optimizer skill. Do not read or expose secrets, auth.json, cookies, tokens, SQLite state, logs, caches, sessions, plugin downloads, or browser state. Run powershell -NoProfile -ExecutionPolicy Bypass -File \"__CODEX_HOME_TOML_CONTENT__\\scripts\\harness-health.ps1\". If it fails, inspect only the nearest safe summary and relevant maintainable harness files, then make the smallest reversible repair under the Codex home. Do not modify business project code or machine-local auth/provider state. Run verify-global-harness.ps1 after a repair, and run deterministic harness evals only when the failure or repair changes shared harness behavior. Report status, changes, verification results, safe artifact paths, and remaining manual actions."
status = "ACTIVE"
rrule = "FREQ=WEEKLY;BYDAY=MO;BYHOUR=9;BYMINUTE=0;BYSECOND=0"
model = __MODEL_TOML_STRING__
reasoning_effort = "high"
execution_environment = "local"
cwds = [__PROJECT_ROOT_TOML_STRING__]
created_at = __NOW_MS__
updated_at = __NOW_MS__
