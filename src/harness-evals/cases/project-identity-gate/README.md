# Eval Case: Project Identity Gate

Verifies that ambiguous document vaults, template libraries, and skill or asset
collections stay read-only until Codex asks about purpose, architecture
boundaries, final outcome, and allowed change scope.

The cases encode regressions where a salient note, a coherent group of workflow
exports, or one archive must not become the inferred purpose of the directory.
