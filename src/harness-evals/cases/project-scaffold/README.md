# Eval Case: Project Scaffold

Verifies that the global `init-project-harness.ps1` script can create the
standard Codex project scaffold, including smoke docs, code map, project rules,
and a major task plan.
