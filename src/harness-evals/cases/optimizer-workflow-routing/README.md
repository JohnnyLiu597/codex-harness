# Eval Case: Optimizer Workflow Routing

Verifies that `project-harness-optimizer` actively routes hook, agent,
workflow, test-surface, verification, and regression-loop work through the
Codex Workflow Core instead of merely documenting that the components exist.
